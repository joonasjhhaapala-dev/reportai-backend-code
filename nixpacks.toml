[phases.setup]
nixPkgs = ["python311", "gcc", "python311Packages.pip"]

[phases.install]
cmds = ["cd /app && pip install -r requirements.txt --break-system-packages"]

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
